<UserControl x:Class="BetterScan.ScanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BetterScan"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <local:EmptyPathConverter x:Key="EmptyPathConverter"/>
    </UserControl.Resources>


    <Grid VerticalAlignment="Top" HorizontalAlignment="Left" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Button Content="Select Folder" Grid.Row="1" Grid.Column="0" 
                Click="ClickSelectFolder"/>
        <Label Content="Folder Path:"  Grid.Row="2" Grid.Column="0" />
        <Label 
            Content="{Binding TargetFolder,
            Converter={StaticResource EmptyPathConverter}}" 
               Grid.Row="2" Grid.Column="1" />
        <Button Content="Scan"  Grid.Row="1" Grid.Column="1"  Click="ClickScan"/>
    </Grid>
</UserControl>
